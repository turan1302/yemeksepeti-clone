import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : { flex: 1, backgroundColor: "#efefef" },
  scroll_area : { marginBottom: 17 },
  text_area : {
    marginTop: 30,
    paddingHorizontal: 13,
    color: "black",
    fontWeight: "bold",
    fontSize: 17,
  }
});

export default styles;
