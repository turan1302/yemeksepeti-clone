import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : {padding : 20},
  button_area : {padding : 7,borderWidth : 1,borderColor : "gray",borderRadius : 18},
  button_text : {color : "black",fontSize : 15,fontWeight : "bold"}
});

export default styles;
